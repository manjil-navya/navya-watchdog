[build]
builder = "dockerfile"

[deploy]
startCommand = "/bin/sh -c \"uvicorn server:app --host 0.0.0.0 --port $PORT\""
healthcheckPath = "/api/status"
healthcheckTimeout = 30
restartPolicyType = "on_failure"